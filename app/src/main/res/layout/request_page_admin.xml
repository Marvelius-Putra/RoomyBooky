<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background">

    <ImageView
        android:id="@+id/background_mini"
        android:layout_width="370dp"
        android:layout_height="600dp"
        android:
        android:background="@drawable/background_req_page"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"></ImageView>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="370dp"
        android:layout_height="600dp"
        app:layout_constraintBottom_toBottomOf="@id/background_mini"
        app:layout_constraintEnd_toEndOf="@+id/background_mini"
        app:layout_constraintStart_toStartOf="@id/background_mini"
        app:layout_constraintTop_toTopOf="@id/background_mini"></androidx.recyclerview.widget.RecyclerView>

</androidx.constraintlayout.widget.ConstraintLayout>